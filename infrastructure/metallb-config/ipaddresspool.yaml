# infrastructure/metallb-config/ipaddresspool.yaml
# AUTO-GENERATED by scripts/03-bootstrap-flux.sh from .env METALLB_IP_RANGE
# Safe to commit — contains no secrets.
#
# For kind:    172.19.255.200-172.19.255.250  (upper IPv4 range of docker bridge)
# For kubeadm: 203.0.113.10/32               (your server public IP)
#
# This file is applied AFTER MetalLB CRDs are installed (dependsOn: infrastructure)
apiVersion: metallb.io/v1beta1
kind: IPAddressPool
metadata:
  name: default-pool
  namespace: metallb-system
spec:
  addresses:
    - 172.19.255.200-172.19.255.250   # Replaced by bootstrap script
---
apiVersion: metallb.io/v1beta1
kind: L2Advertisement
metadata:
  name: default-l2advert
  namespace: metallb-system
spec:
  ipAddressPools:
    - default-pool
