# apps/mattermost/secrets/sealed-db-credentials.yaml
# ─────────────────────────────────────────────────────────────────────────
# PLACEHOLDER — Replaced by scripts/04-wait-and-seal.sh at bootstrap time.
#
# After sealing, this file will contain a real SealedSecret like:
#
#   apiVersion: bitnami.com/v1alpha1
#   kind: SealedSecret
#   metadata:
#     name: mattermost-db-credentials
#     namespace: mattermost
#   spec:
#     encryptedData:
#       DB_CONNECTION_STRING: AgB3x...  (long encrypted string)
#     template:
#       metadata:
#         name: mattermost-db-credentials
#         namespace: mattermost
#       type: Opaque
#
# The Sealed Secrets controller decrypts this and creates a regular
# Kubernetes Secret named mattermost-db-credentials in the mattermost namespace.
# ─────────────────────────────────────────────────────────────────────────
